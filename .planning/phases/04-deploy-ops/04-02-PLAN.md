---
phase: 04-deploy-ops
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: []
autonomous: false
requirements: [DEPLOY-01]
user_setup:
  - service: github
    why: "D-05 default deploy path: public repo + Git-connected Vercel auto-deploy (portfolio/CI-CD visible). Repo is currently local-only (no remote)."
    dashboard_config:
      - task: "Confirm creating a PUBLIC GitHub repository (or decline for the Vercel CLI fallback)"
        location: "GitHub — created via gh CLI after user confirmation"
  - service: vercel
    why: "Host the static frontend + single serverless function on the free hobby tier; set the live env vars."
    env_vars:
      - name: FIRECRAWL_API_KEY
        source: "Firecrawl dashboard (free tier key) — set in Vercel project env, NOT committed"
      - name: GOOGLE_AI_API_KEY
        source: "Google AI Studio key — set in Vercel project env, NOT committed"
      - name: LLM_PROVIDER
        source: "Literal value 'gemini' — set in Vercel project env"
    dashboard_config:
      - task: "Set FIRECRAWL_API_KEY, GOOGLE_AI_API_KEY, LLM_PROVIDER=gemini; leave ANTHROPIC_API_KEY unset"
        location: "Vercel project — Settings > Environment Variables (or `vercel env add`)"
must_haves:
  truths:
    - "The app is reachable at a public *.vercel.app URL"
    - "The full keyword -> idea list flow works in a real browser on that URL (not just localhost)"
    - "The Vercel project has FIRECRAWL_API_KEY, GOOGLE_AI_API_KEY, and LLM_PROVIDER=gemini set; ANTHROPIC_API_KEY is unset"
    - "No secret is written into any committed file during deploy setup"
    - "The user explicitly confirmed the GitHub-push (public repo) action before it happened, OR chose the Vercel CLI fallback"
  artifacts:
    - path: ".planning/phases/04-deploy-ops/DEPLOY-URL.txt"
      provides: "The recorded public production URL for the perf-verification plan (04-03) to use"
      contains: "https://"
  key_links:
    - from: "Vercel serverless function"
      to: "Firecrawl + Google AI"
      via: "env vars set on the Vercel project"
      pattern: "FIRECRAWL_API_KEY"
    - from: "public URL"
      to: "POST /api/generate"
      via: "real-browser keyword submission returning idea cards"
      pattern: "vercel.app"
---

<objective>
Take the app live on Vercel's free hobby tier (DEPLOY-01). The default path (D-05) is: push the local-only repo to a NEW public GitHub repo, then import into Vercel for Git-connected auto-deploy. Because creating a public GitHub repository is an outward-facing action, this plan gates on an explicit user confirmation BEFORE any remote is created or pushed. If the user declines GitHub, fall back to Vercel CLI (`npx vercel --prod`). Configure the live env vars per D-06 and verify the end-to-end flow in a real browser.

Purpose: Success criterion #1 — the app is accessible at a public Vercel URL and the full keyword -> idea list flow works in a real browser.
Output: A live *.vercel.app production URL, the Vercel env config, and DEPLOY-URL.txt recording the URL for the perf plan.

Depends on 04-01 so the deployed code already contains the 504 client branch and the README.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deploy-ops/04-CONTEXT.md
@.env.example
@astro.config.mjs
@.gitignore

<interfaces>
<!-- Deploy-relevant facts (do not re-derive): -->
<!-- - Repo is LOCAL-ONLY: `git remote -v` is empty; no GitHub remote exists yet. -->
<!-- - Vercel CLI is NOT installed; the fallback path uses `npx vercel` (downloads on demand). -->
<!-- - Build emits ONE consolidated serverless function `_render.func` (nodejs24.x, streaming, maxDuration:60 -->
<!--   from the adapter in astro.config.mjs) plus static assets. output:'static' + prerender=false on the API route. -->
<!-- - .gitignore already excludes .env, .env.*.local, .vercel/; a gitleaks pre-commit hook exists. -->
<!-- - Live env vars to set on Vercel (D-06): FIRECRAWL_API_KEY, GOOGLE_AI_API_KEY, LLM_PROVIDER=gemini. -->
<!--   Leave ANTHROPIC_API_KEY UNSET (astro:env marks all keys optional; orchestrator guards missing keys). -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm deploy method BEFORE any outward-facing action (D-05)</name>
  <read_first>
    - .planning/phases/04-deploy-ops/04-CONTEXT.md (D-05 — confirm-before-push; D-06 — env config)
  </read_first>
  <decision>How to deploy: create a PUBLIC GitHub repo + Vercel Git integration, or use the Vercel CLI directly?</decision>
  <context>
    The repo is currently local-only (no git remote). The default (D-05) is to push to a NEW PUBLIC GitHub repository and import it into Vercel for Git-connected auto-deploy — a natural fit for a portfolio/interview piece with visible CI/CD. Creating a public repo is outward-facing and irreversible-ish (the repo becomes publicly visible), so it requires explicit confirmation. The fallback is the Vercel CLI (`npx vercel --prod`), which needs no GitHub. Either way, NO secret is ever committed — keys are set only in the Vercel project env.
  </context>
  <options>
    <option id="github-vercel">
      <name>Default: Public GitHub repo + Vercel Git import (D-05 default)</name>
      <pros>Visible CI/CD + public code for the portfolio; auto-deploy on every push; cleanest interview story.</pros>
      <cons>Creates a PUBLIC repository (code becomes publicly visible); requires the user's GitHub auth.</cons>
    </option>
    <option id="vercel-cli">
      <name>Fallback: Vercel CLI direct deploy (`npx vercel --prod`)</name>
      <pros>No GitHub required; repo stays private/local; fastest to a live URL.</pros>
      <cons>No public repo artifact; no Git-connected auto-deploy; manual redeploys.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The executor did NOT create any git remote, push any commit, or run any Vercel deploy before this checkpoint resolved
    - The user selected exactly one of: github-vercel OR vercel-cli
    - If github-vercel: the user explicitly acknowledged the repo will be PUBLIC
  </acceptance_criteria>
  <resume-signal>Select: github-vercel or vercel-cli (and, for github-vercel, confirm the repo may be public)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Deploy to Vercel and set live env vars (D-01, D-06)</name>
  <read_first>
    - .env.example (the canonical var names to set on Vercel)
    - .gitignore (confirm .env / .vercel/ excluded before any push)
    - astro.config.mjs (adapter maxDuration:60, output:'static' — the deploy must NOT reintroduce a hybrid assumption)
    - .planning/phases/04-deploy-ops/04-CONTEXT.md (D-06 exact env config)
  </read_first>
  <action>
    Execute the path chosen in Task 1. Under NO circumstances write any real key into a committed file — keys go ONLY into the Vercel project env.

    IF github-vercel:
      1. Before pushing, sanity-check no secret would be committed: `git status --porcelain` (nothing sensitive staged) and `git ls-files | grep -E '^\.env$'` returns nothing.
      2. Create the public repo and push using the gh CLI (user already confirmed public in Task 1), e.g.:
         `gh repo create <name> --public --source=. --remote=origin --push`
         (If gh is not authenticated, this surfaces an auth gate — pause, let the user auth, retry.)
      3. Import into Vercel: either connect the repo in the Vercel dashboard (Git integration) or run `npx vercel link` then `npx vercel --prod`. If gh/Vercel dashboard steps require the user's browser login, surface that as an auth gate rather than fabricating success.
      4. Set env vars on the Vercel project (dashboard Settings > Environment Variables OR CLI): set EXACTLY
         `FIRECRAWL_API_KEY`, `GOOGLE_AI_API_KEY`, and `LLM_PROVIDER=gemini` for the Production environment. Leave `ANTHROPIC_API_KEY` UNSET. Ask the user for the FIRECRAWL/GOOGLE key values at deploy time (do not read them from any local file into a command that could be logged into git; the .env is gitignored and stays local).
      5. Trigger a production deploy (a push, or `npx vercel --prod`) AFTER the env vars are set so the function has its keys.

    IF vercel-cli:
      1. `npx vercel link` (creates .vercel/, which is gitignored) then set env vars:
         `npx vercel env add FIRECRAWL_API_KEY production`, `npx vercel env add GOOGLE_AI_API_KEY production`, `npx vercel env add LLM_PROVIDER production` (value `gemini`). Do NOT add ANTHROPIC_API_KEY.
      2. `npx vercel --prod` to deploy. Capture the printed production URL.

    After a successful deploy, record the production URL to `.planning/phases/04-deploy-ops/DEPLOY-URL.txt` (single line, the `https://...vercel.app` URL) so plan 04-03 can consume it.

    Confirm the emitted deployment is the expected shape: a static frontend + a single serverless function (the adapter produces one consolidated `_render.func`; do NOT expect a per-route `api/generate` function).
  </action>
  <verify>
    <automated>test -f .planning/phases/04-deploy-ops/DEPLOY-URL.txt && grep -q "https://" .planning/phases/04-deploy-ops/DEPLOY-URL.txt && test -z "$(git ls-files | grep -E '^\.env$')"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/04-deploy-ops/DEPLOY-URL.txt` exists and contains a line matching `https://.*vercel.app`
    - `curl -s -o /dev/null -w "%{http_code}" <url from DEPLOY-URL.txt>` returns 200 (the frontend loads)
    - The Vercel project shows FIRECRAWL_API_KEY, GOOGLE_AI_API_KEY, and LLM_PROVIDER set for Production; ANTHROPIC_API_KEY is NOT set (verify via `npx vercel env ls` or the dashboard — record the list, WITHOUT values, in the SUMMARY)
    - `git ls-files | grep -E '^\.env$'` returns nothing (no .env committed during deploy)
    - `git log -p -1 | grep -iE 'fc-[a-zA-Z0-9]{10,}|AIzaSy[a-zA-Z0-9_-]{20,}|sk-ant-[a-zA-Z0-9]{10,}'` returns nothing (the deploy commit, if any, leaked no key)
    - IF github-vercel: `git remote -v` shows an `origin` remote (created only after the Task 1 confirmation)
  </acceptance_criteria>
  <done>The app is live at a public *.vercel.app URL that returns 200; the three D-06 env vars are set (ANTHROPIC unset); no secret was committed; the URL is recorded in DEPLOY-URL.txt.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the live end-to-end flow in a real browser (success criterion #1)</name>
  <read_first>
    - .planning/phases/04-deploy-ops/DEPLOY-URL.txt (the live URL to test)
  </read_first>
  <what-built>A live Vercel deployment (static frontend + single serverless function) with FIRECRAWL_API_KEY, GOOGLE_AI_API_KEY, and LLM_PROVIDER=gemini configured.</what-built>
  <how-to-verify>
    1. Open the URL from `.planning/phases/04-deploy-ops/DEPLOY-URL.txt` in a real browser (not localhost).
    2. Enter a real keyword (e.g., "drone photography") and click Generate.
    3. Confirm: step-labeled progress appears, then 8-12 idea cards render, each with a title, an intent badge, and a rationale.
    4. Confirm the copy/download export buttons are present.
    5. (Optional) Confirm a nonsense/blank keyword under 3 chars shows the inline validation error and makes no request.
  </how-to-verify>
  <acceptance_criteria>
    - The user confirms the full keyword -> idea-list flow works in a real browser on the public URL (not localhost)
    - Idea cards render (not a raw error object or blank screen)
  </acceptance_criteria>
  <resume-signal>Type "approved" if the live flow works, or describe what failed.</resume-signal>
</task>

</tasks>

<verification>
- DEPLOY-URL.txt contains a live https://*.vercel.app URL returning 200.
- Vercel env has the three D-06 vars set, ANTHROPIC unset.
- No secret committed at any point (git ls-files + git log grep clean).
- User confirmed the live browser flow.
</verification>

<success_criteria>
- Success criterion #1: public Vercel URL works end-to-end in a real browser (confirmed at the human-verify checkpoint).
- D-05 honored: the GitHub-public-repo action only happened after explicit user confirmation (or the CLI fallback was used).
</success_criteria>

<output>
After completion, create `.planning/phases/04-deploy-ops/04-02-SUMMARY.md` (record the live URL, the chosen deploy method, and the env-var NAMES set on Vercel — never the values).
</output>
