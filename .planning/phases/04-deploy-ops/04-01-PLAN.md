---
phase: 04-deploy-ops
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - src/scripts/app.ts
  - vercel.json
autonomous: true
requirements: [DEPLOY-03, DEPLOY-04]
must_haves:
  truths:
    - "A reader can clone the repo and reach a working `astro dev` environment following only README.md, with no undocumented steps"
    - "README documents the two free API keys (Firecrawl free tier, Google AI Studio) including the 'never enable billing' warning"
    - "README documents the exact Vercel env vars (FIRECRAWL_API_KEY, GOOGLE_AI_API_KEY, LLM_PROVIDER=gemini) and a free-tier-only deploy guide"
    - "README explains architecture (scrape -> parse -> LLM -> cards) and the swappable-provider design, plus $0 + expected-latency notes"
    - "A Vercel 504 gateway-timeout response shows a friendly 'free tier is busy' message instead of the misleading 'Can't reach the server' network message"
  artifacts:
    - path: "README.md"
      provides: "Portfolio-grade setup, env, free-tier deploy, architecture, and performance docs"
      min_lines: 80
      contains: "## Quickstart"
    - path: "src/scripts/app.ts"
      provides: "Client 504 branch extending the Phase 3 error taxonomy"
      contains: "504"
  key_links:
    - from: "README.md Quickstart"
      to: ".env.example"
      via: "copy .env.example to .env instruction"
      pattern: "\\.env\\.example"
    - from: "src/scripts/app.ts submit handler"
      to: "504 friendly message"
      via: "res.status === 504 branch before res.json()"
      pattern: "504"
---

<objective>
Prepare everything that does NOT require the live Vercel URL: the portfolio-grade README (DEPLOY-03), the client-side 504 / gateway-timeout friendly-error handling (D-04, supports DEPLOY-04 UX), and the DEPLOY-02 key-safety re-confirmation against the current repo state. This work lands and commits BEFORE the deploy plan so the deployed code already contains the 504 branch.

Purpose: A fresh clone must reach a working dev env from the README alone (success criterion #2), the one currently-unhandled failure mode (504) must degrade gracefully, and the "no key ever committed" guarantee (success criterion #4) must be re-confirmed.
Output: README.md (new), an updated src/scripts/app.ts with a 504 branch, an optional vercel.json cleanup, and a recorded key-safety confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deploy-ops/04-CONTEXT.md
@.env.example
@astro.config.mjs
@src/scripts/app.ts

<interfaces>
<!-- Current client error taxonomy the 504 branch must extend. From src/scripts/app.ts. -->
<!-- The submit handler currently does: fetch -> const data = await res.json() -> if(!res.ok) showError(mapError(...)). -->
<!-- A Vercel 504 returns a NON-JSON (HTML) body, so `await res.json()` THROWS and is caught by the outer -->
<!-- catch block, which shows "Can't reach the server — check your connection and try again." That message -->
<!-- is misleading for a timeout. The 504 must be detected from res.status BEFORE res.json() is called. -->

Current submit-handler fetch block (src/scripts/app.ts, ~lines 219-236):
```ts
const res = await fetch('/api/generate', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ keyword }),
});
const data = await res.json(); // 504 body is HTML -> this THROWS today
if (!res.ok) {
  showError(mapError(data.error?.code, data.error?.message));
  return;
}
renderCards(data.ideas as VideoIdea[]);
```

ErrorCode union (src/scripts/app.ts): 'VALIDATION' | 'NO_RESULTS' | 'RATE_LIMITED' | 'UPSTREAM_ERROR' | 'INTERNAL'
mapError(code, backendMessage) — switch over ErrorCode returning a friendly string.
showError(msg) — stops progress, hides results, shows the error region.

Env vars (from .env.example — source of truth for README + Vercel config):
- FIRECRAWL_API_KEY   (Firecrawl free tier, 1,000 credits/month, https://firecrawl.dev)
- GOOGLE_AI_API_KEY   (Google AI Studio free key, https://aistudio.google.com)
- ANTHROPIC_API_KEY   (ONLY needed when LLM_PROVIDER=haiku — leave unset for the free live demo)
- LLM_PROVIDER        (gemini default/free, or haiku for consistent low latency at cost)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add client-side 504 / gateway-timeout branch to app.ts</name>
  <read_first>
    - src/scripts/app.ts (see the exact current submit handler, mapError, and showError — do NOT restructure them)
    - .planning/phases/04-deploy-ops/04-CONTEXT.md (D-04: 504 returns non-JSON, handled purely client-side)
  </read_first>
  <action>
    In src/scripts/app.ts, in the form submit handler's `try` block, add a status check for the Vercel gateway timeout BEFORE the `const data = await res.json();` line (a 504 body is HTML, so calling res.json() on it throws and falls into the outer catch, wrongly showing the network message).

    Insert this branch immediately after the `const res = await fetch(...)` call and BEFORE `const data = await res.json();`:
    ```ts
    if (res.status === 504) {
      showError('The free tier is busy right now — please try again in a moment.');
      return;
    }
    ```
    Rationale to preserve in a brief comment: "504 = Vercel killed the function at maxDuration; body is non-JSON, so branch before res.json(). The function cannot catch its own maxDuration kill — handled purely client-side (D-04)."

    Do NOT change the existing `mapError` switch, the `RATE_LIMITED` message, the outer `catch` network message, or the `finally` block. This is an additive branch only. The exact wording of the message is at your discretion but MUST contain the substring "free tier is busy".

    Optional (D-08, Claude's discretion): the branch may also handle 503 the same way if you judge it useful; not required.
  </action>
  <verify>
    <automated>grep -q "504" src/scripts/app.ts && grep -q "free tier is busy" src/scripts/app.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "res.status === 504" src/scripts/app.ts` returns >= 1
    - `grep -c "free tier is busy" src/scripts/app.ts` returns >= 1
    - The 504 branch appears in the source BEFORE the `await res.json()` line (verify by reading the submit handler ordering)
    - `npm run build` exits 0 (Astro build succeeds — no TS/type errors introduced)
    - The existing outer `catch` network message "Can't reach the server" is still present and unchanged
    - The existing `mapError` switch (VALIDATION/NO_RESULTS/RATE_LIMITED/UPSTREAM_ERROR/INTERNAL) is unchanged
  </acceptance_criteria>
  <done>A 504 response short-circuits to a friendly "free tier is busy" message before res.json() is called; the build passes; no other error paths changed.</done>
</task>

<task type="auto">
  <name>Task 2: Write the portfolio-grade README.md</name>
  <read_first>
    - .env.example (canonical env var list — mirror EXACTLY into the README env section)
    - astro.config.mjs (adapter maxDuration:60, output:'static', astro:env server schema — describe accurately)
    - package.json (scripts: dev = "astro dev", build = "astro build"; node engine >=22.12.0)
    - .planning/PROJECT.md (§ "What This Is", § "Context", § "Key Decisions" — source the architecture + $0 story)
    - .planning/phases/04-deploy-ops/04-CONTEXT.md (D-02, D-06, D-07 — README scope, expected-performance note, Vercel env config)
    - src/pages/api/generate.ts (the scrape -> parse -> LLM -> ideas pipeline to describe in architecture section)
  </read_first>
  <action>
    Create README.md at the repo root (does not exist yet). Portfolio/interview-grade prose. It MUST contain these exact section headings (grep-checkable) and content:

    `# YouTube Demand Miner` — title.

    Overview paragraph + core value: "Turn what people are already searching for on Google into demand-grounded YouTube video ideas." Describe the keyword-in -> demand-grounded-ideas-out flow.

    `## Quickstart` — the fresh-clone path with these EXACT steps, in order, so a reader with zero context reaches a working dev env with NO undocumented steps:
      1. `git clone <repo-url> && cd <repo>`
      2. `npm install`  (node >= 22.12.0 required — state this)
      3. Copy the env template: `cp .env.example .env`
      4. Fill in `FIRECRAWL_API_KEY` and `GOOGLE_AI_API_KEY` in `.env` (link to the "Getting free API keys" section below)
      5. `npm run dev`  (runs `astro dev`) and open the printed localhost URL
    The Quickstart heading string must be exactly `## Quickstart`.

    `## Getting free API keys` — the free-key walkthrough:
      - Firecrawl: sign up at https://firecrawl.dev, free tier = 1,000 credits/month, copy the key into `FIRECRAWL_API_KEY`.
      - Google AI Studio: create a key at https://aistudio.google.com (no credit card), free tier, copy into `GOOGLE_AI_API_KEY`.
      - Include the explicit warning VERBATIM containing the phrase: "never enable billing on that Google project — it removes the free tier". This exact substring "never enable billing" MUST appear.

    `## Environment variables` — a table/list mirroring .env.example EXACTLY: `FIRECRAWL_API_KEY`, `GOOGLE_AI_API_KEY`, `ANTHROPIC_API_KEY` (only needed when `LLM_PROVIDER=haiku`), and `LLM_PROVIDER` (default `gemini` = free; `haiku` = paid, needs ANTHROPIC_API_KEY). State that keys are server-only env vars, never committed, never PUBLIC_-prefixed.

    `## Deploy to Vercel (free tier)` — a free-tier-only deploy guide covering: push repo to GitHub, import into Vercel (Git-connected auto-deploy), and set exactly these three env vars on the Vercel project: `FIRECRAWL_API_KEY`, `GOOGLE_AI_API_KEY`, `LLM_PROVIDER=gemini` (leave `ANTHROPIC_API_KEY` unset for the free demo). Mention the Vercel CLI fallback `npx vercel --prod`. State that it runs as a static Astro frontend + a single serverless function on Vercel's hobby (free) tier, no paid services.

    `## How it works` (architecture) — walk through the pipeline exactly as an interview explanation: browser POSTs the keyword to a single serverless function (`/api/generate`) -> Firecrawl `/v2/search` fetches top Google organic results (titles + snippets) as the demand signal -> a token-capped demand parser -> an LLM (default Gemini 2.5 Flash via the Vercel AI SDK) synthesizes 8-12 ranked ideas (title + intent + rationale, Zod-validated) -> rendered as cards. Explain the swappable-provider design: switch `LLM_PROVIDER` between `gemini` and `haiku` with no code change.

    `## Performance & cost` — the $0 + expected-latency note (D-02): runs at $0 on free tiers; typical warm end-to-end ~15-20s; occasional free-tier latency spikes are a known limitation, not a defect; `LLM_PROVIDER=haiku` is the swap for anyone wanting consistent low latency at cost. The exact substring "LLM_PROVIDER=haiku" MUST appear in this section.

    Screenshots/GIFs are optional (your discretion). Section ordering beyond the required headings is your discretion, but ALL the exact heading strings above must be present.
  </action>
  <verify>
    <automated>test -f README.md && grep -q "## Quickstart" README.md && grep -q "never enable billing" README.md && grep -q "LLM_PROVIDER=haiku" README.md && grep -q "## How it works" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f README.md` succeeds (file exists at repo root)
    - `wc -l < README.md` returns >= 80
    - `grep -c "## Quickstart" README.md` returns >= 1
    - `grep -c "## Getting free API keys" README.md` returns >= 1
    - `grep -c "## Environment variables" README.md` returns >= 1
    - `grep -c "## Deploy to Vercel (free tier)" README.md` returns >= 1
    - `grep -c "## How it works" README.md` returns >= 1
    - `grep -c "## Performance & cost" README.md` returns >= 1
    - `grep -c "never enable billing" README.md` returns >= 1
    - `grep -c "cp .env.example .env" README.md` returns >= 1
    - `grep -c "LLM_PROVIDER=haiku" README.md` returns >= 1
    - `grep -c "FIRECRAWL_API_KEY" README.md` returns >= 1 AND `grep -c "GOOGLE_AI_API_KEY" README.md` returns >= 1
    - README contains no real API key values: `grep -E "fc-[a-zA-Z0-9]{10,}|AIzaSy[a-zA-Z0-9_-]{20,}|sk-ant-[a-zA-Z0-9]{10,}" README.md` returns nothing (only placeholders like `fc-placeholder` allowed)
  </acceptance_criteria>
  <done>README.md exists at repo root with all required headings, mirrors .env.example, includes the billing warning and the Haiku latency swap, and a fresh reader can reach a working dev env from it alone.</done>
</task>

<task type="auto">
  <name>Task 3: Re-confirm key safety (DEPLOY-02) and clean up moot vercel.json route key</name>
  <read_first>
    - .gitignore (confirms .env, .env.*.local, .vercel/ are excluded)
    - vercel.json (the per-route maxDuration key that no longer matches the emitted function name)
    - astro.config.mjs (adapter maxDuration:60 is the authoritative source)
    - .planning/phases/04-deploy-ops/04-CONTEXT.md (D-08 discretion on vercel.json; DEPLOY-02 re-confirmation)
  </read_first>
  <action>
    Two parts:

    (A) Key-safety re-confirmation (DEPLOY-02 — a VERIFICATION sub-task, NOT a claimed requirement in this plan's frontmatter). Run and record the results of:
      - `git ls-files | grep -E '^\.env$'` — MUST return nothing (no tracked .env). Only `.env.example` should be tracked.
      - `git log -p -- . | grep -iE 'fc-[a-zA-Z0-9]{10,}|AIzaSy[a-zA-Z0-9_-]{20,}|sk-ant-[a-zA-Z0-9]{10,}'` — MUST return nothing (no real Firecrawl/Google/Anthropic key value ever committed). Placeholder strings in .env.example (fc-placeholder, AIzaSy-placeholder, sk-ant-placeholder) are expected and fine — they are not real keys.
      - Confirm `.gitignore` contains `.env` and `.vercel/`, and that the gitleaks pre-commit hook exists: `test -f .git/hooks/pre-commit`.
    Record the command outputs in the plan SUMMARY (this is the honest evidence for success criterion #4). If ANY check finds a real key or a tracked .env, STOP and flag it as a blocker — do not proceed to deploy.

    (B) vercel.json cleanup (D-08, Claude's discretion — do this since the key is confirmed moot). The adapter's `maxDuration: 60` in astro.config.mjs is authoritative; the emitted function is a single consolidated `_render.func`, NOT `src/pages/api/generate.ts`, so the vercel.json `functions` key targets a non-existent function name. Either:
      - Remove the now-moot `functions` block from vercel.json (leaving a minimal valid `{ "$schema": "https://openapi.vercel.sh/vercel.json" }` or deleting the file), OR
      - Leave it and add a one-line note — but prefer removing the dead key to keep the repo interview-clean.
    Whichever you choose, `npm run build` must still succeed afterward.
  </action>
  <verify>
    <automated>test -z "$(git ls-files | grep -E '^\.env$')" && test -z "$(git log -p -- . | grep -iE 'fc-[a-zA-Z0-9]{10,}|AIzaSy[a-zA-Z0-9_-]{20,}|sk-ant-[a-zA-Z0-9]{10,}')" && test -f .git/hooks/pre-commit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files | grep -E '^\.env$'` returns nothing (no tracked .env file)
    - `git log -p -- . | grep -iE 'fc-[a-zA-Z0-9]{10,}|AIzaSy[a-zA-Z0-9_-]{20,}|sk-ant-[a-zA-Z0-9]{10,}'` returns nothing (no real key value in any commit)
    - `grep -q '^\.env$' .gitignore` succeeds AND `grep -q '\.vercel/' .gitignore` succeeds
    - `test -f .git/hooks/pre-commit` succeeds (gitleaks guard present)
    - vercel.json either no longer contains `"src/pages/api/generate.ts"` OR the file was deleted (the moot per-route key is removed) — verify with `! grep -q 'src/pages/api/generate.ts' vercel.json 2>/dev/null`
    - `npm run build` exits 0 after any vercel.json change
    - Key-safety command outputs are recorded in the SUMMARY as DEPLOY-02 evidence
  </acceptance_criteria>
  <done>Repo is confirmed free of any committed .env or real key (evidence recorded), the gitignore + pre-commit guards are verified present, and the dead vercel.json route key is removed with a still-passing build.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0 with all three tasks applied.
- README.md exists with every required heading and the billing + Haiku strings.
- app.ts has a 504 branch before res.json().
- git history + ls-files show no real key or tracked .env.
</verification>

<success_criteria>
- Fresh-clone-from-README reachability (success criterion #2) is satisfied by the Quickstart section (grep-verified headings + `cp .env.example .env`).
- The 504 failure mode degrades to a friendly message (supports DEPLOY-04 / success criterion #3 UX).
- Key-safety (success criterion #4) is re-confirmed with recorded evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/04-deploy-ops/04-01-SUMMARY.md` (include the recorded key-safety command outputs as DEPLOY-02 evidence).
</output>
